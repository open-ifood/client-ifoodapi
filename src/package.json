{
  "name": "@open-ifood/api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn tsc",
    "start": "NODE_ENV=production && npm run build && node ./dist/server.js",
    "dev": "NODE_ENV=dev && ts-node-dev --respawn ./server.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^7.0.4",
    "tsc": "^2.0.3",
    "typedoc": "^0.22.10"
  }
}
