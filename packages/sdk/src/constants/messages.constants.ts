const jwtTokenExpired: string = 'token expired';
const jwtTokenNotFound: string = 'no jwt token';
const failureMessages: Array<string> = [jwtTokenExpired, jwtTokenNotFound];

export default { failureMessages, jwtTokenExpired, jwtTokenNotFound };
